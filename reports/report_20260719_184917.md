> **AI-generated (EU AI Act Art. 50).** The prose in this report was written by an open-source LLM (Qwen/Qwen2.5-1.5B-Instruct, run locally via HuggingFace transformers) from the agent's structured run data. Findings are produced by deterministic detection oracles, not by the model.

## Web Application Security Assessment Report

### Summary
This assessment was conducted on an application running at `http://127.0.0.1:5000` with the profile set to `pyapp`. The target has been deemed authorized, and the status of the test is marked as 'done'. There are **6 injection points** identified across the application, out of which **19 payloads have been fired**, resulting in **7 confirmed findings**. Among these, **6 findings** were held due to lack of evidence from a headless browser, while **1 finding** was confirmed through a differential Oracle.

### Scope & Authorization
The assessment adhered strictly to the given scope and was executed with explicit authorization. All actions taken were within the bounds defined by the policy.

### Confirmed Findings
| Attack Class | Technique | Oracle | Confidence |
|--------------|------------|--------|-----------|
| SQL Injection | Tautology | Differential | Medium |
| Cross-Site Scripting (XSS) | Stored | Browser Execution | Medium |
| Cross-Site Scripting (XSS) | Reflected | Browser Execution | Medium |

These findings indicate that the application contains potential vulnerabilities related to SQL Injection and XSS attacks, specifically stored and reflected types. The findings were confirmed using differentials and browser execution checks, respectively.

### Governance & Caveats
- **Payloads Held**: None of the payloads were actually fired during the test.
- **Oracle Usage**: The use of Oracle checks was limited to confirming whether the payload could be executed in a browser environment.
- **Confidence Levels**: The confidence levels reflect the degree of certainty derived from the available evidence. For instance, tautological conditions were confirmed based on the divergence between true and false outcomes, whereas browser execution checks were performed to verify JavaScript execution capabilities.

Given the nature of this assessment, no further action is required beyond documenting the findings and ensuring appropriate remediation measures are implemented.

---
## Ground-truth facts (deterministic)
- Target: `http://127.0.0.1:5000`  · profile: `pyapp`  · authorized: True
- Injection points: 6  · payloads fired: 19  · held by governance: 6
- **Confirmed exploits: 7**  · by class: {'sqli': 1, 'xss': 6}
    - sqli/tautology at `sqli` via `differential` (medium): true vs false condition diverged ("' OR 1=2--" returned differently)
    - xss/stored at `xss` via `browser_execution` (medium): payload reflected unescaped into HTML (reflected-XSS candidate); JS execution not verified without a headless browser
    - xss/reflected at `xss` via `browser_execution` (medium): payload reflected unescaped into HTML (reflected-XSS candidate); JS execution not verified without a headless browser
    - xss/stored at `xss` via `browser_execution` (medium): payload reflected unescaped into HTML (reflected-XSS candidate); JS execution not verified without a headless browser
    - xss/reflected at `xss` via `browser_execution` (medium): payload reflected unescaped into HTML (reflected-XSS candidate); JS execution not verified without a headless browser
    - xss/stored at `comment` via `browser_execution` (medium): payload reflected unescaped into HTML (reflected-XSS candidate); JS execution not verified without a headless browser
    - xss/stored at `comment` via `browser_execution` (medium): payload reflected unescaped into HTML (reflected-XSS candidate); JS execution not verified without a headless browser
