> **AI-generated (EU AI Act Art. 50).** The prose in this report was written by an open-source LLM (Qwen/Qwen2.5-1.5B-Instruct, run locally via HuggingFace transformers) from the agent's structured run data. Findings are produced by deterministic detection oracles, not by the model.

### Summary

- **Fired Payloads**: 24
- **Held Payloads**: 10
- **Confirmed Findings**: 7

### Scope & Authorization

The assessment was conducted on an application running locally at `http://127.0.0.1:5000`, targeting version pyapp with an authorized status. The scope includes all injection points identified within the application, totaling 7 points.

### Confirmed Findings

| Class       | Technique           | Oracle                | Confidence |
|-------------|---------------------|------------------------|------------|
| XSS (Stored) | Reflected            | Browser Execution     | Medium     |
| XSS (Stored) | Stored               | Browser Execution     | Medium     |
| Sqli        | Tautology             | Differential          | Medium     |

These findings indicate that there are stored and reflected cross-site scripting (XSS) vulnerabilities present in the application. Additionally, a SQL Injection vulnerability has been detected through a tautology-based test.

### Governance & Caveats

This assessment confirms the presence of XSS vulnerabilities, both stored and reflected, which could lead to malicious scripts being executed if exploited. However, due to the nature of the tests used (not involving a headless browser), the JavaScript execution cannot be verified directly. Therefore, while the findings are confirmed, they should be treated with caution until further verification using a more robust method such as a headless browser.

---
## Ground-truth facts (deterministic)
- Target: `http://127.0.0.1:5000`  · profile: `pyapp`  · authorized: True
- Injection points: 7  · payloads fired: 24  · held by governance: 10
- **Confirmed exploits: 7**  · by class: {'xss': 6, 'sqli': 1}
    - xss/stored at `comment` via `browser_execution` (medium): payload reflected unescaped into HTML (reflected-XSS candidate); JS execution not verified without a headless browser
    - xss/stored at `comment` via `browser_execution` (medium): payload reflected unescaped into HTML (reflected-XSS candidate); JS execution not verified without a headless browser
    - sqli/tautology at `sqli` via `differential` (medium): true vs false condition diverged ("' OR 1=2--" returned differently)
    - xss/stored at `xss` via `browser_execution` (medium): payload reflected unescaped into HTML (reflected-XSS candidate); JS execution not verified without a headless browser
    - xss/reflected at `xss` via `browser_execution` (medium): payload reflected unescaped into HTML (reflected-XSS candidate); JS execution not verified without a headless browser
    - xss/stored at `xss` via `browser_execution` (medium): payload reflected unescaped into HTML (reflected-XSS candidate); JS execution not verified without a headless browser
    - xss/reflected at `xss` via `browser_execution` (medium): payload reflected unescaped into HTML (reflected-XSS candidate); JS execution not verified without a headless browser
