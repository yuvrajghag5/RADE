# ── Offensive IT-Tester — dependencies ───────────────────────────────
# Pinned to lower bounds; tested on Python 3.11+.

# Data pipeline (preprocess/) + baseline model (models/)
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3            # persist trained models -> models/*.pkl

# Config + Layer 1 (authorization) + Layer 2 recon profiles
PyYAML>=6.0

# Layer 2 (reconnaissance) — LIVE crawler (required: recon logs in + crawls the target)
requests>=2.31
beautifulsoup4>=4.12

# Self-owned live sandbox target (sandbox/target_app.py) — used when DVWA/Docker
# is unavailable; recon crawls it exactly like DVWA.
flask>=3.0

# Notebooks — preprocess/analysis.ipynb and models/baseline.ipynb
notebook>=7.0
ipykernel>=6.25        # kernel that executes the notebook cells
nbformat>=5.9          # read/write notebooks
nbconvert>=7.0         # execute notebooks programmatically

# Tests (planned, tests/)
pytest>=8.0
