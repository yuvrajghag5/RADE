# ══ Offensive IT-Tester — dependencies ═══════════════════════════════════════
# Use Python 3.11 or 3.12 (they have CUDA torch wheels; Python 3.14 does NOT).
#
#   py -3.11 -m venv .venv311
#   .venv311\Scripts\activate            # Windows  (source .venv311/bin/activate on macOS)
#
# ┌─ GPU install (recommended — NVIDIA card) ─────────────────────────────────┐
# │  1) install the CUDA build of torch FIRST (driver 12.x/13.x -> cu124):     │
# │       pip install torch --index-url https://download.pytorch.org/whl/cu124 │
# │  2) then the rest:                                                          │
# │       pip install -r requirements.txt                                       │
# │  verify: python -c "import torch; print(torch.cuda.is_available())" -> True │
# │  (4 GB VRAM fits Qwen2.5-1.5B in fp16; keep that model — 3B needs >4 GB.)   │
# └────────────────────────────────────────────────────────────────────────────┘
#
#   CPU-only: skip step 1 — just `pip install -r requirements.txt` (pulls CPU torch;
#   reports are slow, so set model: Qwen/Qwen2.5-0.5B-Instruct in config/llm.yaml).
# ─────────────────────────────────────────────────────────────────────────────

# ── Data pipeline (preprocess/) + baseline model (models/) ──
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3

# ── Config + Layer 1 (authorize) + Layer 2 (live recon) + Layer 5 (execution) ──
PyYAML>=6.0
requests>=2.31
beautifulsoup4>=4.12

# ── Self-owned sandbox target (sandbox/) + Web UI (webui.py) ──
# (Flask installs Werkzeug, used by the sandbox test server too)
flask>=3.0

# ── Agent orchestration (src/agent/graph.py) ──
langgraph>=0.2

# ── Layer 7 report — local open-source LLM via HuggingFace ──
# torch: installed from the CUDA index above for GPU; on CPU this pulls the CPU build.
torch>=2.2
transformers>=4.44
accelerate>=0.33

# ── Notebooks (baseline.ipynb, unit_test.ipynb, demo_walkthrough.ipynb) ──
notebook>=7.0
ipykernel>=6.25
nbformat>=5.9
nbconvert>=7.0
