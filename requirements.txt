# ── Offensive IT-Tester — dependencies ───────────────────────────────
# Pinned to lower bounds; tested on Python 3.11+.
 
# Layer 1 (authorization) + config
PyYAML>=6.0
 
# Layer 2 (reconnaissance)
requests>=2.31
beautifulsoup4>=4.12
 
# Notebook + baseline model (Layer 3 intelligence / analysis)
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
notebook>=7.0          # to run notebooks/analysis.ipynb
nbformat>=5.9          # notebook build/execution tooling
 
# Tests
pytest>=8.0
 